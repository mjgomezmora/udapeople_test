version: 2.1

jobs:
  build-frontend:
    docker:
      - image: cimg/node:13.8.0
    steps:
      - checkout
      - restore_cache:
          keys: [frontend-build]
      - run: npm i
      - run: npm run lint
      - save_cache:
          paths: [frontend/src]
          key: frontend-build

  build-backend:
    docker:
      - image: cimg/node:13.8.0
    steps:
      - checkout
      - restore_cache:
          keys: [backend-build]
      - run: npm i
      - run: npm run lint
      - save_cache:
          paths: [backend/src]
          key: backend-build

workflows:
  say-hello-workflow:
    jobs:
      - build-frontend
      - build-backend
